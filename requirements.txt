Flask==3.0.0
Flask-CORS==4.0.0
Pillow==10.1.0
numpy>=1.25.0
opencv-python-headless==4.8.1.78
requests==2.31.0
gunicorn==21.2.0
setuptools>=65.0.0
# Note: torch, torchvision, ultralytics, and sahi are installed via NIXPACKS_INSTALL_CMD
# to ensure proper dependency resolution and avoid conflicts
